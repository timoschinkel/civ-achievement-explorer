{
  "name": "civ-achievements-explorer",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "web-dev-server --node-resolve --open --watch --app-index assets/index.html",
    "bundle": "node scripts/build.js"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "esbuild": "^0.28.0",
    "glob": "^13.0.6",
    "lit": "^3.3.2",
    "typescript": "^5.0.4"
  }
}
